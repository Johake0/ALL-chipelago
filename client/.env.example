VITE_API_URL=http://localhost:4000

# GitHub Pages project-page deploys need this set to '/your-repo-name/' at
# build time (see SETUP.md). Leave unset (defaults to '/') for a custom
# domain or a GitHub user/org page.
VITE_BASE_PATH=/
